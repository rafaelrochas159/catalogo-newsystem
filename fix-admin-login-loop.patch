*** Begin Patch
*** Add File: app/admin/login/layout.tsx
+/**
+ * Layout específico para a página de login do admin.
+ * Ao existir este arquivo, o Next.js não aplicará o AdminLayout
+ * sobre /admin/login, evitando loops de redirecionamento.
+ */
+export default function LoginLayout({
+  children,
+}: {
+  children: React.ReactNode;
+}) {
+  return <>{children}</>;
+}
*** End Patch

*** Begin Patch
*** Update File: app/admin/layout.tsx
@@
-import { useRouter } from 'next/navigation';
+import { useRouter, usePathname } from 'next/navigation';
@@
 export default function AdminLayout({
   children,
 }: {
   children: React.ReactNode;
 }) {
   const router = useRouter();
+  const pathname = usePathname();
@@
   useEffect(() => {
     checkAuth();
-  }, []);
+  }, [pathname]);
@@
       const { data: { session } } = await supabase.auth.getSession();
       
-      if (!session) {
-        router.push('/admin/login');
-        return;
-      }
+      if (!session) {
+        // só redireciona se não estivermos na página de login
+        if (pathname !== '/admin/login') {
+          router.push('/admin/login');
+        }
+        return;
+      }
@@
     } catch (error) {
       console.error('Auth error:', error);
-      router.push('/admin/login');
+      if (pathname !== '/admin/login') {
+        router.push('/admin/login');
+      }
     } finally {
       setIsLoading(false);
     }
   };
*** End Patch
